 地质读图智能体开发规划

 上下文

 目标：构建一个多模态地质读图智能体，能够：
 1. 处理结构化文本报告并构建知识图谱（已完成）
 2. 读取地质图并做分析（待实现）
 3. 集成网络检索功能（部分完成）

 当前状态：
 - ✅ 知识图谱构建已完成：PDF → 三元组抽取 → Neo4j 存储
 - ✅ 已有基础的 ReAct Agent（llama_index/04Agent_Context_Maintenance.py）
 - ✅ 已有 Google Custom Search 网络检索工具
 - ⬜ vision_process/ 目录为空，图像分析能力待实现
 - ⬜ 14 张地质图图片（data/gouli_map/*.JPG）待处理

 ---
 核心架构设计

 智能体架构（基于 LangGraph）

 ┌─────────────────────────────────────────────────────────────────┐
 │                     用户查询入口                                 │
 │         "分析沟里地区的成矿地质条件"                              │
 │         "这张地质图中有哪些矿床？它们与断层的关系是什么？"         │
 └──────────────────────────┬──────────────────────────────────────┘
                            ▼
 ┌─────────────────────────────────────────────────────────────────┐
 │                    LangGraph 智能体编排层                        │
 │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
 │  │ 意图识别节点 │→│ 任务规划器  │→│ 并行执行 (工具调用)     │ │
 │  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
 │                              │                                   │
 │                    ┌─────────┴─────────┐                        │
 │                    ▼                   ▼                        │
 │         ┌─────────────────┐  ┌─────────────────┐                │
 │         │ 图谱查询子图    │  │ 图像分析子图    │                │
 │         └────────┬────────┘  └────────┬────────┘                │
 └────────────────────────┼────────────────┼────────────────────────┘
                          │                │
         ┌────────────────┼────────────────┼────────────────────┐
         │                │                │                    │
         ▼                ▼                ▼                    ▼
 ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
 │ 知识图谱工具 │ │ 视觉分析工具 │ │ 网络检索工具 │ │ 文件/代码工具 │
 │ (Neo4j KG)   │ │ (Vision)     │ │ (WebSearch)  │ │ (Shell/Py)   │
 └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘

 ---
 功能模块规划

 模块 1：视觉分析模块 (vision_process/)

 核心功能：
 1. 地质图预处理
   - 图像加载与格式转换
   - 颜色校正与增强
   - 图例区域检测与识别
 2. 图例识别与符号提取
   - 图例符号模板匹配
   - 地质符号分类（断层线、岩性符号、矿点标记等）
   - OCR 识别图例文字说明
 3. 地质要素提取
   - 断层线/褶皱轴迹检测（边缘检测 + 霍夫变换）
   - 岩性单元分割（基于颜色/纹理）
   - 矿点/采样点位置识别（模板匹配）
 4. 空间关系分析
   - 矿点与断层的空间关系（距离、方向）
   - 岩性单元邻接关系
   - 构造线密度分析
 5. 多模态融合
   - 视觉提取的实体 → 知识图谱节点
   - 图谱实体 → 图像空间定位
   - 联合查询："显示 F1 断层附近 2km 内的所有矿点"

 技术选型建议：

 ┌──────────────┬────────────────────────┬────────────────┐
 │     功能     │         推荐库         │      理由      │
 ├──────────────┼────────────────────────┼────────────────┤
 │ 基础图像处理 │ OpenCV                 │ 成熟、性能好   │
 ├──────────────┼────────────────────────┼────────────────┤
 │ OCR          │ PaddleOCR              │ 中文识别效果好 │
 ├──────────────┼────────────────────────┼────────────────┤
 │ 目标检测     │ YOLOv8 / DETR          │ 地质符号检测   │
 ├──────────────┼────────────────────────┼────────────────┤
 │ 语义分割     │ Segment Anything (SAM) │ 岩性单元分割   │
 ├──────────────┼────────────────────────┼────────────────┤
 │ 多模态理解   │ InternVL / Qwen-VL     │ 地质图整体理解 │
 └──────────────┴────────────────────────┴────────────────┘

 文件结构：
 vision_process/
 ├── __init__.py
 ├── config.py              # 视觉模型配置
 ├── image_loader.py        # 图像加载与预处理
 ├── legend_extractor.py    # 图例识别
 ├── feature_detector.py    # 地质要素检测
 ├── spatial_analyzer.py    # 空间关系分析
 ├── kg_integration.py      # 与知识图谱融合
 └── main.py               # 视觉分析入口

 ---
 模块 2：智能体编排层（LangGraph）

 核心状态定义：
 class AgentState(TypedDict):
     messages: Annotated[list[BaseMessage], add_messages]
     intent: str                    # detected intent
     kg_results: Optional[list]     # 知识图谱查询结果
     vision_results: Optional[dict] # 视觉分析结果
     web_results: Optional[list]    # 网络检索结果
     final_answer: Optional[str]

 图结构设计：
                     ┌──────────────┐
                     │   START      │
                     └──────┬───────┘
                            ▼
                     ┌──────────────┐
                     │ 意图识别     │── 简单问候 → [回复] → END
                     └──────┬───────┘
                            │
         ┌──────────────────┼──────────────────┐
         ▼                  ▼                  ▼
 ┌───────────────┐  ┌───────────────┐  ┌───────────────┐
 │ 知识图谱查询   │  │ 地质图分析    │  │ 网络信息检索   │
 └───────┬───────┘  └───────┬───────┘  └───────┬───────┘
         │                  │                  │
         └──────────────────┼──────────────────┘
                            ▼
                     ┌───────────────┐
                     │  结果融合/生成 │
                     └───────┬───────┘
                            ▼
                     ┌───────────────┐
                     │    END        │
                     └───────────────┘

 节点函数：
 - intent_classifier: 基于 LLM 判断查询类型（KG 查询/图像分析/网络检索/混合）
 - kg_query_node: 从 Neo4j 检索知识
 - vision_analysis_node: 分析地质图图像
 - web_search_node: 网络检索补充信息
 - synthesize_node: 融合多源信息生成最终回答

 文件位置：
 agent/
 ├── __init__.py
 ├── graph.py              # LangGraph 主图定义
 ├── nodes.py              # 各节点实现
 ├── state.py              # 状态定义
 ├── tools/
 │   ├── kg_tool.py        # 知识图谱查询工具
 │   ├── vision_tool.py    # 视觉分析工具
 │   └── web_search_tool.py # 网络检索工具
 └── main.py               # 智能体入口

 ---
 模块 3：知识图谱增强

 当前 KG 的局限：
 - 仅支持文本 → 三元组 → 图谱
 - 缺少空间信息（坐标、位置关系）
 - 不支持复杂推理

 增强方向：
 1. 空间索引集成
   - 在 Neo4j 中添加 Spatial 插件
   - 实体添加 coordinates 属性（经纬度）
   - 支持空间查询："查找 X 矿点 5km 内的所有断层"
 2. 混合检索
   - 结合向量检索（语义相似度）+ 图遍历（结构关系）
   - RAG 工作流升级：检索 → 图遍历 →  rerank → 生成
 3. 推理能力
   - 基于规则的推理：A 赋存于 B, B 位于 C → A 位于 C
   - 基于嵌入的推理：TransE/RotatE 预测潜在关系

 ---
 模块 4：网络检索增强

 当前实现（llama_index/04Agent_Context_Maintenance.py）：
 - ✅ Google Custom Search 已集成
 - ⚠️ 仅返回前 2 条结果，缺少深度分析

 改进方向：
 1. 多源检索
   - 添加 Tavily API（专为 AI 设计的搜索引擎）
   - 学术搜索：arXiv、CNKI 等
   - 专业数据库：Mindat（矿物数据库）、USGS（地质调查）
 2. 检索后处理
   - 内容摘要提取
   - 相关性打分与过滤
   - 与 KG 实体对齐
 3. 智能路由
   - 简单事实 → 直接网络检索
   - 专业地质问题 → 优先 KG 查询
   - 最新研究 → 网络 + 学术搜索

 ---
 实现优先级

 阶段 1：基础智能体框架（1-2 周）

 - 创建 agent/ 目录结构
 - 实现 LangGraph 状态机（意图识别 → 工具调用 → 合成）
 - 集成现有知识图谱查询工具
 - 集成现有网络检索工具

 阶段 2：视觉分析模块 MVP（2-3 周）

 - 地质图图像加载与预处理
 - 基础边缘检测与断层线提取
 - 简单 OCR 识别图例文字
 - 将提取结果与 KG 实体关联

 阶段 3：多模态融合（2 周）

 - 图像提取的实体 → KG 节点
 - 空间关系图谱化
 - 联合查询接口

 阶段 4：增强与优化（持续）

 - 空间索引集成
 - 多源网络检索
 - 推理能力增强
 - 用户界面（可选：Gradio/Streamlit）

 ---
 推荐新增功能

 除了多模态和网络检索，智能体还可以增加：

 1. 报告自动生成

 - 基于 KG 和图像分析结果自动生成地质报告
 - 模板：区域地质背景 → 成矿条件 → 找矿预测

 2. 交互式问答

 - 支持多轮对话（已有基础）
 - 支持引用来源（KG 三元组/图像区域/网页链接）

 3. 不确定性表达

 - 置信度输出："F1 断层（置信度 87%）可能控制矿体分布"
 - 多假设分析：展示多种可能的地质解释

 4. 可视化输出

 - KG 子图可视化（PyVis/Plotly）
 - 图像标注叠加（断层、矿点位置）
 - 空间分析结果热力图

 5. 批处理模式

 - 批量分析多张地质图
 - 区域对比分析

 ---
 技术栈建议

 ┌────────────┬────────────────────┬──────────────────┐
 │    类别    │        推荐        │       备注       │
 ├────────────┼────────────────────┼──────────────────┤
 │ 智能体框架 │ LangGraph          │ 已有基础         │
 ├────────────┼────────────────────┼──────────────────┤
 │ 知识图谱   │ Neo4j + LlamaIndex │ 已有             │
 ├────────────┼────────────────────┼──────────────────┤
 │ 视觉处理   │ OpenCV + PaddleOCR │ 轻量、中文友好   │
 ├────────────┼────────────────────┼──────────────────┤
 │ 目标检测   │ YOLOv8             │ 快速、可部署本地 │
 ├────────────┼────────────────────┼──────────────────┤
 │ 多模态模型 │ Qwen-VL / InternVL │ 中文支持好       │
 ├────────────┼────────────────────┼──────────────────┤
 │ 网络检索   │ Tavily + Google    │ 互补             │
 ├────────────┼────────────────────┼──────────────────┤
 │ 嵌入模型   │ Zhipu AI (已有)    │ 继续用           │
 ├────────────┼────────────────────┼──────────────────┤
 │ LLM        │ Gemini (已有)      │ 继续用           │
 └────────────┴────────────────────┴──────────────────┘

 ---
 验证方案

 单元测试

 - 视觉模块：测试图像加载、OCR 准确率
 - KG 工具：测试 Cypher 查询正确性
 - 意图识别：测试分类准确率

 端到端测试

 - 测试用例 1："沟里地区有哪些主要矿床？" → KG 查询
 - 测试用例 2："分析这张地质图中的断层系统" → 视觉分析
 - 测试用例 3："东天山最新的找矿进展" → 网络检索
 - 测试用例 4："结合地质图分析黄山铜镍矿的控矿因素" → 多模态融合

 评估指标

 - 三元组抽取准确率（vs 人工标注）
 - 断层线检测召回率/IoU
 - 意图识别准确率
 - 端到端问答人工评估

 ---
 风险与依赖

 ┌──────────────────────────┬──────┬──────────────────────────┐
 │           风险           │ 影响 │         缓解措施         │
 ├──────────────────────────┼──────┼──────────────────────────┤
 │ 地质图质量差（扫描模糊） │ 高   │ 图像增强预处理           │
 ├──────────────────────────┼──────┼──────────────────────────┤
 │ OCR 识别率低（手写体）   │ 中   │ 多引擎融合               │
 ├──────────────────────────┼──────┼──────────────────────────┤
 │ 目标检测数据不足         │ 高   │ 人工标注 + 迁移学习      │
 ├──────────────────────────┼──────┼──────────────────────────┤
 │ API 调用成本             │ 中   │ 本地模型优先             │
 ├──────────────────────────┼──────┼──────────────────────────┤
 │ 多模态对齐困难           │ 中   │ 基于空间位置的启发式规则 │
 └──────────────────────────┴──────┴──────────────────────────┘

 ---
 下一步行动

 1. 确认需求优先级：先做视觉分析还是先完善智能体框架？
 2. 确认技术选型：是否需要用大模型做端到端地质图理解？
 3. 数据准备：是否需要标注地质图数据用于训练？